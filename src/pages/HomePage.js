import Navbar from "../components/Navbar"
import { getNameLogin } from "../utils"

const Home = () => {
    return (
        <section>
            <Navbar />
        </section>
    )
}

export default Home